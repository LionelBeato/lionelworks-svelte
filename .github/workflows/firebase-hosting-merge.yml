
# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

# name: Deploy to Firebase Hosting on merge
# 'on':
#   push:
#     branches:
#       - main
# jobs:
#   build_and_deploy:
#     runs-on: ubuntu-latest
#     steps:
#     - name: Deploy Firebase Function
#       uses: danielmillan/pipelines-firebase-functions@1.0.1

    # - name: Firebase CLI Deployment
    #   uses: doonstore/firebase-deploy@v1
    #   env:
    #     FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
    #     FIREBASE_PROJECT: ${{ secrets.FIREBASE_PROJECT }}
      # - uses: actions/checkout@v3
      # - name: Use Node.js 16.x
      #   uses: actions/setup-node@v3
      # - uses: actions/checkout@v2
      # - run: npm install firebase-tools && firebase deploy
      # - uses: FirebaseExtended/action-hosting-deploy@v0
      #   with:
      #     repoToken: '${{ secrets.GITHUB_TOKEN }}'
      #     firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_FDS3SD }}'
      #     channelId: live
      #     projectId: fds3sd


name: Firebase
on:
  push:
    branches:
    - main
jobs:
  main:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Check out code
      uses: actions/checkout@v3
    - uses: actions/setup-node@v3
      with:
        node-version: 16
    - run: npm install